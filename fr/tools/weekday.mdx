---
title: "Wochentag"
api: "GET https://tools.innochat.ch/weekday"
description: "Diese Funktion ermittelt den Wochentag zu einem angegebenen Datum. Zum Beispiel liefert das Datum '2024-10-07' das JSON-Ergebnis: `{'weekday': 'Monday'}`. Ideal für Anwendungen, die Datumslogik, Kalenderfunktionen oder automatisierte Datumsauswertung benötigen."
---

### Tool-Parameter

```json
{
  "type": "object",
  "properties": {
    "date": {
      "type": "string",
      "description": "Das Datum im ISO-8601-Format. Zum Beispiel 2022-06-15 (15. Juni 2022)."
    }
  },
  "required": ["date"]
}
```
Include default metadata: False

### Pfad
<ParamField path="date" type="string" required />
  
### Antwort
<ResponseField name="weekday" type="string" /> <RequestExample>

```bash Curl
curl --location --request GET 'https://tools.innochat.ch/weekday?date=2024-10-10' \
--header 'Authorization: Bearer <token>' \
--data-raw ''

```

```python
import requests

url = 'https://tools.innochat.ch/weekday?date=2024-10-10'
headers = {
    'Authorization': 'Bearer <token>'
}

response = requests.get(url, headers=headers)

if response.status_code == 200:
    print("Request successful!")
    print(response.text)
else:
    print("Request failed with status code:", response.status_code)
    print(response.text)

```

```ts typescript
const axios = require("axios");

const url = "https://tools.innochat.ch/weekday?date=2024-10-10";
const headers = {
  Authorization: "Bearer <token>",
};

axios
  .post(url, { headers })
  .then((response) => {
    console.log("Request successful!");
    console.log(response.text);
  })
  .catch((error) => {
    console.error("Request failed:", error);
  });

```
</RequestExample>

<ResponseExample>

```json Response
{
  "weekday": "Thursday"
}
```

</ResponseExample>